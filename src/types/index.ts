export type CategoryType = {
  title: string
  image: any
}

export type DestinationType = {
  id: number;
  title: string,
  duration: string,
  distance: string,
  weather: string,
  price: number,
  shortDescription: string,
  longDescription: string,
  image: any
}

